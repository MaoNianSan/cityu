# Generated outputs

Historical fast/full outputs are intentionally excluded from this audited package.
Run `python main.py --mode fast --workers 1` and then the full command after the
A--H audit passes. See `audit/AUDIT_REPORT.md` for the invalidation scope.
